global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "kairos-worker"
    static_configs:
      - targets: ["worker:2112"]
  - job_name: "pushgateway"
    honor_labels: true
    static_configs:
      - targets: ["pushgateway:9091"]
